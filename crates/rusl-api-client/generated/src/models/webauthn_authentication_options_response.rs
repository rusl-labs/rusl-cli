/*
 * Rusl API
 *
 * API for the Rusl application
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

use serde_with::serde_as;

/// WebauthnAuthenticationOptionsResponse : Options for `navigator.credentials.get()`.  ## Encoding  The `challenge` is 32 random bytes, base64url-encoded (no padding). Decode to `ArrayBuffer` before passing to the WebAuthn browser API.  The `challenge_token` is an opaque server-signed token. Store in BFF session and send back verbatim with the verify request.  Uses discoverable credentials (resident keys) — no user identifier or `allowCredentials` list is needed. The device selects the credential.
#[serde_as]
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WebauthnAuthenticationOptionsResponse {
    /// 32 random bytes, base64url-encoded (no padding). Decode to ArrayBuffer before passing to navigator.credentials.get().
    #[serde_as(as = "serde_with::base64::Base64")]
    #[serde(rename = "challenge")]
    pub challenge: Vec<u8>,
    /// Server-signed opaque token. Store in BFF session, send back verbatim with the verify request. Do not decode.
    #[serde(rename = "challenge_token")]
    pub challenge_token: String,
    /// Relying party ID (domain)
    #[serde(rename = "rpId")]
    pub rp_id: String,
    /// Timeout in milliseconds
    #[serde(rename = "timeout", skip_serializing_if = "Option::is_none")]
    pub timeout: Option<i32>,
    #[serde(rename = "userVerification", skip_serializing_if = "Option::is_none")]
    pub user_verification: Option<UserVerification>,
}

impl WebauthnAuthenticationOptionsResponse {
    /// Options for `navigator.credentials.get()`.  ## Encoding  The `challenge` is 32 random bytes, base64url-encoded (no padding). Decode to `ArrayBuffer` before passing to the WebAuthn browser API.  The `challenge_token` is an opaque server-signed token. Store in BFF session and send back verbatim with the verify request.  Uses discoverable credentials (resident keys) — no user identifier or `allowCredentials` list is needed. The device selects the credential.
    pub fn new(
        challenge: Vec<u8>,
        challenge_token: String,
        rp_id: String,
    ) -> WebauthnAuthenticationOptionsResponse {
        WebauthnAuthenticationOptionsResponse {
            challenge,
            challenge_token,
            rp_id,
            timeout: None,
            user_verification: None,
        }
    }
}
///
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum UserVerification {
    #[serde(rename = "preferred")]
    Preferred,
    #[serde(rename = "required")]
    Required,
    #[serde(rename = "discouraged")]
    Discouraged,
}

impl Default for UserVerification {
    fn default() -> UserVerification {
        Self::Preferred
    }
}
